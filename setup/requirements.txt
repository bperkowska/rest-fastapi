# FastAPI i Uvicorn
fastapi[standard]

# Pydantic (walidacja)
pydantic


# Database (SQLAlchemy + PostgreSQL)
sqlalchemy
psycopg2-binary


# Environment variables
pydantic-settings>=2.0.0
python-dotenv

# Authentication & Security
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
pyjwt==2.8.0

# Email validation
pydantic[email]

# Testing
pytest

# CORS
# (built-in w FastAPI, ale dla pewności)

# Optional: Async HTTP client
httpx